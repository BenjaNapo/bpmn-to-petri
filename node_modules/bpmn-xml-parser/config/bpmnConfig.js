const camundaModeler = {
  collaborationLabel: "bpmn:collaboration",
  messageFlowLabel: "bpmn:messageFlow",
  processLabel: "bpmn:process",
  participantLabel: "bpmn:participant",
  versionTag: "camunda:versionTag"
};

const bpmn2 = {
  collaborationLabel: "collaboration",
  messageFlowLabel: "messageFlow",
  processLabel: "process",
  participantLabel: "participant",
  versionTag: "camunda:versionTag"
};

let bpmnConfig = camundaModeler;
export default bpmnConfig;
